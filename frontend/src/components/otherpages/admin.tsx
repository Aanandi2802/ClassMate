import React from 'react';
const Admin = () => {

    return (
        <>This is team memeber's section. Since logged in user is an admin, this page is showing. I have added User Management portion for admin below</>
    );
};

export default Admin;

